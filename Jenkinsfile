pipeline {
    agent {
        kubernetes {
            yamlFile 'build-pod.yaml'
            defaultContainer 'jnlp-slave'
        }
    }
    stages {
        stage('terraform init'){

        }
    }
}